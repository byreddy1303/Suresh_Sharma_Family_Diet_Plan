{
  "name": "suresh-sharma-family-diet-plan",
  "version": "1.0.0",
  "private": true,
  "description": "Static diet plan with a Groq-backed Q&A and voice backend for deployment on Vercel.",
  "scripts": {
    "check": "node --check api/ask.js && node --check api/transcribe.js && node --check api/health.js && node --check api/_lib/config.js && node --check api/_lib/document.js && node --check api/_lib/groq.js && node --check api/_lib/multipart.js && node --check api/_lib/prompts.js && node --check assistant.js",
    "backend:smoke": "node scripts/backend-smoke-test.js"
  },
  "engines": {
    "node": ">=20"
  }
}
